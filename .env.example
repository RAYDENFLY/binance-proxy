# Environment Variables Example
# Copy this file to .env for local development

# Node Environment
NODE_ENV=production

# Port - Will be overridden by platform (Easypanel, Vercel, etc)
# For local development only
PORT=3002

# For Easypanel deployment:
# - ONLY set NODE_ENV=production in Easypanel dashboard
# - DO NOT set PORT (Easypanel will auto-set it)
# - App will bind to 0.0.0.0 for external access

# Optional: Add API key for authentication (if needed in future)
# API_KEY=your_secret_key_here
